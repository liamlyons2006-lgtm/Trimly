{
  "version": "v1",
  "hooks": [
    {
      "name": "Sync changes to GitHub",
      "trigger": "Stop",
      "description": "When an agent turn completes, commit and push working-tree changes to GitHub — but only after scanning staged content for secrets. Aborts the push (without committing) if a secret or env file is staged. No-ops when the tree is clean.",
      "action": {
        "type": "command",
        "command": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/github-autosync.ps1",
        "timeout": 120
      }
    }
  ]
}
